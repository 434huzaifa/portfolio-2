[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Serve all routes from the static export
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
